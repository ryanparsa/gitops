apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: frontend-beta
  namespace: argocd
spec:
  project: default
  source:
    repoURL: 'https://github.com/ryanparsa/gitops.git'
    targetRevision: HEAD
    path: 'frontend/overlays/beta'
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: frontend-beta
  syncPolicy:
    automated:
      prune: true
      selfHeal: true